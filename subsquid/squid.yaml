manifestVersion: subsquid.io/v0.1
name: polkadot-issuance-sqd-v2
version: 1
description: Substrate squid template
build: 
deploy:
  addons:
    postgres: 
    rpc:
      - asset-hub-polkadot.http
  processor:
    cmd:
      - sqd
      - process:prod
  api:
    cmd:
      - sqd
      - serve:prod
